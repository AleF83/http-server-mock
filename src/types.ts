export interface ServerMockOptions {
  name: string;
  port: number;
}

export interface ServerMockInfo {
  id: string;
  name: string;
  port: number;
}

export class ServerResponse {
  url = '/';
}
